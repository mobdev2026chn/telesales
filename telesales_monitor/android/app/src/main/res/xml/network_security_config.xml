<?xml version="1.0" encoding="utf-8"?>
<!-- Release: HTTPS only, system CAs only. Debug builds use src/debug/res/xml/network_security_config.xml,
     which additionally allows cleartext to local dev backends and user-installed CAs. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
